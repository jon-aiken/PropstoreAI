You are reviewing the input fields for a Propstore auction lot prior to catalog copy generation. Identify any fields that are required for this category but are missing, empty, or likely ambiguous, and that will therefore constrain generation quality.

<lot_context>{{lot_context}}</lot_context>

<p1_fields>{{p1_fields}}</p1_fields>

<p2_fields>{{p2_fields}}</p2_fields>

<p3_items>{{p3_items}}</p3_items>

<p4_items>{{p4_items}}</p4_items>

For each concern identify: which paragraph it affects (P1, P2, P3, or P4), the field name, the item_id if it is an item-level concern, and a brief plain-English description of the issue and its likely impact on copy quality. If there are no concerns, return an empty array. Respond only with the JSON array and absolutely no other text.
