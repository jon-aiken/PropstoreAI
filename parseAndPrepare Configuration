{"metadata": {"name": "parseAndPrepare", "description": "Parses the raw harness payload, strips field metadata, applies the requirements matrix, and sets all derived variables for downstream generation steps."}, "blockStyle": {"label": "Parse & Prepare"}, "transitionType": "controlled", "configurationSections": [{"items": [{"type": "inputVariable", "label": "Raw Payload", "helpText": "The raw JSON string posted by the harness. In production this should be set to {{payload}} (the launch variable). In testing, paste a full example payload JSON string here.", "variable": "payload", "placeholder": "{{payload}}"}], "title": "Payload Input"}]}
